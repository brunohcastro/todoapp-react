import styled from 'styled-components';

const ToggleStatusWrapper = styled.div`
  display: inline-flex;
  width: 40px;
  padding: 0 11px;
  .pretty {
    margin: 0;
  }
`;

export default ToggleStatusWrapper;