import styled from 'styled-components';

const TodoCounter = styled.span`
  text-align: left;
  font-size: 14px;
  font-weight: 300;
  line-height: 22px;
  float: left;
  padding: 4px 10px;
`;

export default TodoCounter;
